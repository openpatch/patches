import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Hooks/useBreakpoints" />

# Usage

```ts
import { useBreakpoints } from "@openpatch/patches/hooks";

const value = useBreakpoints(["default", "small", "large"]);
```
