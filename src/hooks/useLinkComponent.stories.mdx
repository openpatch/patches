import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Hooks/useLinkComponent" />

# Usage

```ts
import { useLinkComponent } from "@openpatch/patches/hooks";

const value = useLinkComponent();
```
