import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Hooks/useFirstRender" />

# Usage

```ts
import { useFirstRender } from "@openpatch/patches/hooks";

const value = useFirstRender();
```