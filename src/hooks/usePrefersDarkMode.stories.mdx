import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Hooks/usePrefersDarkMode" />

# Usage

```ts
import { usePrefersDarkMode } from "@openpatch/patches/hooks";

const value = usePrefersDarkMode();
```
