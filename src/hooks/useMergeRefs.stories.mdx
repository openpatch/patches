import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Hooks/useMergeRefs" />

# Usage

```ts
import { useMergeRefs } from "@openpatch/patches/hooks";

const value = useMergeRefs(ref);
```
